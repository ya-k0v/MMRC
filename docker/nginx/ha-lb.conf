map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

log_format upstream '$remote_addr - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent" '
                    'upstream="$upstream_addr" rt="$upstream_response_time"';

upstream mmrc_cluster {
    least_conn;
    server mmrc-replica:80 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 5120M;
    access_log /var/log/nginx/access.log upstream;

    location / {
        proxy_pass http://mmrc_cluster;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
    }

    location /streams/ {
        proxy_pass http://mmrc_cluster;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_buffering off;
        proxy_cache off;
        add_header Cache-Control no-cache;
        add_header Access-Control-Allow-Origin *;
    }
}

# HTTPS server block (requires certificates mounted at /etc/nginx/ssl/)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     client_max_body_size 5120M;
#
#     location / {
#         proxy_pass http://mmrc_cluster;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
#
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 86400s;
#     }
#
#     location /streams/ {
#         proxy_pass http://mmrc_cluster;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_buffering off;
#         proxy_cache off;
#         add_header Cache-Control no-cache;
#         add_header Access-Control-Allow-Origin *;
#     }
# }
