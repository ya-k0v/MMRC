name: Build and Push Docker Image

on:
  push:
    branches: [ docker, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: docker

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Server image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: pingwin1900/mmrc:latest
          platforms: linux/amd64

      - name: Build and push Optimizer image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.optimizer
          push: true
          tags: pingwin1900/mmrc-optimizer:latest
          platforms: linux/amd64

      - name: Build and push Streamer image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.streamer
          push: true
          tags: pingwin1900/mmrc-streamer:latest
          platforms: linux/amd64

      - name: Build and push Converter image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.converter
          push: true
          tags: pingwin1900/mmrc-converter:latest
          platforms: linux/amd64
